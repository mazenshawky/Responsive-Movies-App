import 'package:flutter/material.dart';

class TabletMoviesScreen extends StatelessWidget {
  const TabletMoviesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
